// TODO ticket 7 — HUD: risograph misregistration numerals + labels.
// Populates #hud-alt / #hud-speed / #hud-bearing / #hud-stall / #hud-fail.

export function init(state) {
  // TODO ticket 7
}

export function update(state, dt) {
  // TODO ticket 7 — recompute state.hud values from state.plane / state.level.
}

export function render(state, alpha) {
  // TODO ticket 7 — write state.hud into the DOM roots.
}
