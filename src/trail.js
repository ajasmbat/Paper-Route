// TODO ticket 9 — ribbon trail: pink, widens with speed.

export function init(state) {
  // TODO ticket 9 — allocate ribbon geometry / material.
}

export function update(state, dt) {
  // TODO ticket 9 — push a new ribbon segment behind state.plane.position.
}

export function render(state, alpha) {
  // TODO ticket 9
}
