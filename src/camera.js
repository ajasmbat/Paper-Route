// TODO ticket 5 — camera: chase spring + FOV swell + roll damping.

export function init(state) {
  // TODO ticket 5
}

export function update(state, dt) {
  // TODO ticket 5 — spring state.camera.chasePos / chaseQuat toward the plane.
}

export function render(state, alpha) {
  // TODO ticket 5 — apply interpolated chasePos/chaseQuat + fov to state.three.camera.
}
