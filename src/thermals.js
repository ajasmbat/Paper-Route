// TODO ticket 4 — thermals: cylinders + falloff + shimmer + specks.

export function init(state) {
  // TODO ticket 4
}

export function update(state, dt) {
  // TODO ticket 4 — apply thermal lift to state.plane.velocity.
}

export function render(state, alpha) {
  // TODO ticket 4
}
