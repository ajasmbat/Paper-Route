// TODO ticket 3 — world: instanced buildings + fog + spatial-grid collision.

export function init(state) {
  // TODO ticket 3
}

export function update(state, dt) {
  // TODO ticket 3 — spatial-grid collision queries.
}

export function render(state, alpha) {
  // TODO ticket 3
}
