// TODO ticket 8 — input: pointer-lock mouse + WASD/QE with ramping.
// Writes state.input.pitch / roll / throttleIsh in -1..1.

export function init(state) {
  // TODO ticket 8 — attach pointer-lock + keyboard listeners.
}

export function update(state, dt) {
  // TODO ticket 8 — ramp raw input toward target values at the fixed step.
}
