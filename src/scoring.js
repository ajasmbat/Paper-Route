// TODO ticket 11 — scoring + landing + failure states + integration polish.

export function init(state) {
  // TODO ticket 11
}

export function update(state, dt) {
  // TODO ticket 11 — check landing box / bounds / stall + write state.hud.failMsg.
}
